// Create a function which accepts radius of circle and returns area of circle

function areaOfCircle(radius: number): number{
    var PI: number = 3.1415;
    return PI * (radius * radius);
}

console.log(areaOfCircle(10));
console.log(areaOfCircle(21));
console.log(areaOfCircle(51));